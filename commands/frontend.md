---
description: Design or implement a distinctive responsive frontend with accessible interaction.
argument-hint: page or component goal, target files, content, states, and visual constraints
---

# Build Frontend

Preserve all text following `/frontend` as task input. When empty, use the active
UI request and available design context.

Resolve the configuration root as the parent of this command file's directory.

1. Read its `skills/frontend-design/SKILL.md`.
2. Inspect existing components, routes, design tokens, assets, responsive
   behavior, state flow, and accessibility conventions.
3. Follow the skill's visual, interaction, implementation, validation, and
   output contract.
4. Edit only when the task requests implementation; otherwise return a design
   specification or critique.

Report changed surfaces, interaction states, responsive and accessibility
evidence, and checks concisely. Do not invent assets, functionality, or browser
validation that was not supplied or run.
