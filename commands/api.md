---
description: Design or evolve a stable API contract with explicit compatibility and errors.
argument-hint: consumers, operations, schemas, constraints, and compatibility window
---

# Design API

Preserve all text following `/api` as task input. When empty, use the active
interface proposal or request its consumers and objective.

Resolve the configuration root as the parent of this command file's directory.

1. Read its `skills/api-designer/SKILL.md`.
2. Inspect existing contracts, implementations, consumers, schemas, errors,
   authorization, versioning, tests, and operational constraints.
3. Follow the skill's contract design, compatibility, decision boundaries,
   quality gates, and output contract.
4. Remain read-only unless implementation or specification-file edits are
   explicitly requested.

Return concise operations, schemas, errors, compatibility decisions, and
verification needs. Distinguish accepted requirements from assumptions and
never claim consumer compatibility without evidence.
