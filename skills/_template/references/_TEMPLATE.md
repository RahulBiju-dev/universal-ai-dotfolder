# Reference Title

Add a reference only when the guidance is long, rarely needed, or optional. If
every use of the skill needs it, it belongs in `SKILL.md` instead.

## Section

- Lead each section with the decision it supports.
- Prefer tables, checklists, and worked examples over narrative.
- Keep local links resolvable from this file's directory.

## Constraints

- Name this file explicitly from `SKILL.md` so the router knows it exists.
- Do not restate the contract's workflow, gates, or output rules.
- Avoid unfinished markers; the workspace validator rejects them.
